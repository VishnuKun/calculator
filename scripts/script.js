//split calculator box into grids of columns and rows
function createGrid(18) {
    //access the box 
    let box = document.querySelector("#box");
    box.style.display = "grid";
    //split it into columns
    box.style.
}